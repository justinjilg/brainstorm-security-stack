{
  "id": "auth-handler",
  "title": "JWT Authentication Handler",
  "description": "Issue and verify JWTs for multi-tenant access. ECDSA P-256 signing, RBAC with MSP/security/compliance roles, short-lived access tokens, refresh rotation. ALL types, interfaces, and logic must be in a SINGLE Go file with NO imports from example.com or yourmodule.",
  "implementAgent": "jordan-auth",
  "currentPhase": "integrate",
  "status": "completed",
  "artifacts": {
    "spec": [
      "docs/specs/auth-handler.md"
    ],
    "design": [
      "docs/design/auth-handler.md"
    ],
    "implement": [
      "src/auth/auth.go",
      "src/auth/auth.go",
      "src/auth/auth.go",
      "src/auth/auth.go"
    ],
    "review": [
      "docs/reviews/auth-handler-review.md",
      "docs/reviews/auth-handler-review.md",
      "docs/reviews/auth-handler-review.md",
      "docs/reviews/auth-handler-review.md"
    ],
    "test": [
      "src/auth/auth_test.go"
    ],
    "compliance": [
      "docs/compliance/auth-handler-evidence.md"
    ],
    "integrate": [
      "infra/ci/auth-handler.yaml"
    ]
  },
  "reviewResults": [
    {
      "reviewer": "casey-apisec",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0.107625
    },
    {
      "reviewer": "taylor-qa",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0
    },
    {
      "reviewer": "alex-crypto",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0
    },
    {
      "reviewer": "casey-apisec",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0
    },
    {
      "reviewer": "taylor-qa",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0
    },
    {
      "reviewer": "alex-crypto",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0
    },
    {
      "reviewer": "casey-apisec",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0
    },
    {
      "reviewer": "taylor-qa",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0
    },
    {
      "reviewer": "alex-crypto",
      "verdict": "critical",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **RefreshTokenHandler uses wrong verification method** (line 119): `h.TokenService.VerifyAccessToken(refreshToken, tenantID)` — you're verifying a refresh token with the access token verification method. This is fundamentally broken.",
        "",
        "2. **No request body size limits** on any handler. `json.NewDecoder(r.Body).Decode(&req)` will happily consume gigabytes of JSON, opening you to trivial DoS.",
        "",
        "3. **TokenRequest.Custom accepts arbitrary data** (line 38) without validation, then blindly merges it into JWT claims (lines 69-71). I can inject reserved claims like `exp`, `iat`, or `iss` to manipulate token behavior.",
        ""
      ],
      "cost": 0
    },
    {
      "reviewer": "casey-apisec",
      "verdict": "pass",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **TokenIssuanceHandler** properly validates required fields (UserID, TenantID, Roles, SessionID, IssuedBy) before token generation",
        "2. **validateCustomClaims** correctly prevents injection of reserved JWT claims through the custom claims map",
        "3. **decodeJSONLimited** implements request size limiting (64KB) to prevent memory exhaustion attacks",
        "4. **RBACMiddleware** enforces both authentication (Bearer token) and tenant isolation (X-Tenant-ID header)",
        "5. **RefreshTokenHandler** properly validates refresh tokens against tenant context before rotation",
        "6. Token rotation is implemented in refresh flow, invalidating old refresh tokens"
      ],
      "cost": 0.088395
    },
    {
      "reviewer": "taylor-qa",
      "verdict": "pass",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **TokenIssuanceHandler** properly validates required fields (UserID, TenantID, Roles, SessionID, IssuedBy) before token generation",
        "2. **validateCustomClaims** correctly prevents injection of reserved JWT claims through the custom claims map",
        "3. **decodeJSONLimited** implements request size limiting (64KB) to prevent memory exhaustion attacks",
        "4. **RBACMiddleware** enforces both authentication (Bearer token) and tenant isolation (X-Tenant-ID header)",
        "5. **RefreshTokenHandler** properly validates refresh tokens against tenant context before rotation",
        "6. Token rotation is implemented in refresh flow, invalidating old refresh tokens"
      ],
      "cost": 0
    },
    {
      "reviewer": "alex-crypto",
      "verdict": "pass",
      "findings": [
        "",
        "## Findings",
        "",
        "1. **TokenIssuanceHandler** properly validates required fields (UserID, TenantID, Roles, SessionID, IssuedBy) before token generation",
        "2. **validateCustomClaims** correctly prevents injection of reserved JWT claims through the custom claims map",
        "3. **decodeJSONLimited** implements request size limiting (64KB) to prevent memory exhaustion attacks",
        "4. **RBACMiddleware** enforces both authentication (Bearer token) and tenant isolation (X-Tenant-ID header)",
        "5. **RefreshTokenHandler** properly validates refresh tokens against tenant context before rotation",
        "6. Token rotation is implemented in refresh flow, invalidating old refresh tokens"
      ],
      "cost": 0
    }
  ],
  "buildResults": [
    {
      "passed": false,
      "error": "src/auth/auth.go:13:2: missing go.sum entry for module providing package github.com/golang-jwt/jwt/v5 (imported by github.com/justinjilg/brainstorm-security-stack/src/auth); to add:\n\tgo get github.com/justinjilg/brainstorm-security-stack/src/auth",
      "attempt": 1
    },
    {
      "passed": true,
      "error": "",
      "attempt": 2
    },
    {
      "passed": false,
      "error": "src/auth/auth.go:13:2: missing go.sum entry for module providing package github.com/golang-jwt/jwt/v5 (imported by github.com/justinjilg/brainstorm-security-stack/src/auth); to add:\n\tgo get github.com/justinjilg/brainstorm-security-stack/src/auth",
      "attempt": 1
    },
    {
      "passed": true,
      "error": "",
      "attempt": 2
    },
    {
      "passed": true,
      "error": "",
      "attempt": 1
    },
    {
      "passed": true,
      "error": "",
      "attempt": 1
    }
  ],
  "outputPaths": {
    "spec": "docs/specs/auth-handler.md",
    "design": "docs/design/auth-handler.md",
    "implement": [
      "src/auth/auth.go"
    ],
    "review": "docs/reviews/auth-handler-review.md",
    "test": [
      "src/auth/auth_test.go"
    ],
    "compliance": "docs/compliance/auth-handler-evidence.md",
    "integrate": [
      "infra/ci/auth-handler.yaml"
    ]
  }
}
